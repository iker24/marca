{% extends '::base.html.twig' %}

{% block body -%}
    <h1>categorias list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Desccat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('categorias_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descCat }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('categorias_show', { 'id': entity.id }) }}">Show</a>
                    </li>
                    <li>
                        <a href="{{ path('categorias_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('categorias_new') }}">
                Crear una nueva categoria
            </a>
        </li>
    </ul>
    {% endblock %}
