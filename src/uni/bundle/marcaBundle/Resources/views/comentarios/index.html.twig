{% extends '::base.html.twig' %}

{% block body -%}
    <h1>comentarios list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titulocom</th>
                
                <th>Fechacom</th>
                <th>Nick</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('comentarios_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tituloCom }}</td>
                
                <td>{{ entity.fechaCom }}</td>
                <td>{{ entity.nick }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('comentarios_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('comentarios_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('comentarios_new') }}">
             Crear un nuevo comentario
            </a>
        </li>
    </ul>
    {% endblock %}
